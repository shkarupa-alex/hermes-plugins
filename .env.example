# Copy these placeholders into the active Hermes profile's .env and replace them.
VK_COMMUNITY_TOKEN=<VK_COMMUNITY_ACCESS_TOKEN>

# Used by `hermes vk probe-formatting --peer-id ...` during the live capability spike.
VK_GROUP_ID=<VK_COMMUNITY_ID>
VK_TEST_PEER_ID=<YOUR_PRIVATE_TEST_PEER_ID>
